# Copy to collector/.env (gitignored) and fill in.
# Generate the token in the app: Activity page → Collector setup → Generate token.
PGAIOS_INGEST_TOKEN=

# Optional overrides
PGAIOS_API_BASE_URL=https://personal-growth-ai-os-teal.vercel.app
PGAIOS_DEBUG=0
